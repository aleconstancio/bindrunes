name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - uses: oven-sh/setup-bun@735343b667d3e6f658f44d0eca948eb6282f2b76 # v2.0.2
        with:
          bun-version-file: .tool-versions
          cache: true

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Audit dependencies
        run: npm audit --omit=dev --audit-level=high || echo "WARN: audit found issues (non-blocking for now)"

      - name: Lint
        run: bun run lint

      - name: Type check
        run: bunx turbo run check --filter=bindrunes --filter=bindrunes-kit

      - name: Build
        run: bun run build

      - name: Test
        run: bun run test:ci

      - name: Bundle size check
        run: bun run size

      - name: Verify examples build
        run: |
          for dir in examples/*/; do
            if [ -f "${dir}package.json" ]; then
              echo "Building ${dir}..."
              (cd "$dir" && bun install --frozen-lockfile 2>/dev/null && bun run build 2>/dev/null) || echo "WARN: ${dir} build failed (may need library linked)"
            fi
          done
