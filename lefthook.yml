pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,ts,svelte,json}"
      run: bun run lint
    format:
      glob: "*.{js,ts,svelte,json,css}"
      run: bun run format

pre-push:
  commands:
    validate:
      run: bun run validate
